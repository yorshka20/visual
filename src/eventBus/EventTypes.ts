/**
 * 消息类型
 */
export enum EventTypes {
  MOVE = 'move',
  MOUSEDOWN = 'mousedown',

  HOVER = 'hover',
  CLICK = 'click',

  SHAPE = 'shape',
}

/**
 * 消息命名空间
 */
export enum Namespace {
  INTERACTION = 'interaction',
  INIT = 'init',
}
